// The file which defines the setup for the sidebar 
// contians the following components: Pet, task, taskForm, taskList, timer
// will have exit (close the chrome extension), and minimize (convert to popup) options


export {}